import { Hash } from 'lucide-react';
import { worldStore } from '../../core/ECSStore';
import { useStore } from '../../hooks/useStore';
export default function CodexPanel() { const shards = useStore(worldStore, (s) => s.memoryShards); return <section className="bg-black/40 backdrop-blur-xl border border-white/5 p-6 rounded-[2.5rem] pointer-events-auto h-72 overflow-hidden flex flex-col shadow-2xl"><div className="text-[8px] text-white/20 uppercase flex items-center gap-2 mb-4 font-black"><Hash size={10} /> Codex_Live_Stream</div><div className="flex-1 overflow-y-auto space-y-3 no-scrollbar font-black">{shards.map((shard) => <article key={shard.id} className="text-[9px] border-l-2 border-amber-500/20 bg-amber-500/5 p-2 rounded-lg animate-in"><div className="flex justify-between mb-1"><span className="text-amber-400/60 font-bold uppercase">{shard.id.slice(0, 8)}</span><span className="opacity-10 text-[7px]">{shard.tags?.[0]}</span></div><p className="text-[10px] text-white/40 italic line-clamp-2">&quot;{shard.content || 'Memory Fragment Data'}&quot;</p></article>)}{shards.length === 0 && <div className="text-[9px] text-white/10 uppercase text-center mt-12 italic">Searching Memory Shards...</div>}</div></section>; }
