import { Ghost } from 'lucide-react';
import { worldStore } from '../../core/ECSStore';
import { useStore } from '../../hooks/useStore';
export default function GhostPanel() { const pets = useStore(worldStore, (s) => s.ghostPets); const entries = Object.entries(pets); return <section className="bg-black/60 border border-purple-500/10 rounded-[2rem] p-5 pointer-events-auto backdrop-blur-xl"><div className="text-[9px] uppercase tracking-widest text-purple-300/40 mb-4 flex items-center gap-2 font-black"><Ghost size={14} /> Ghost Evolution</div><div className="space-y-3">{entries.map(([id, pet]) => <div key={id} className="rounded-3xl bg-purple-500/5 border border-purple-400/10 p-4 text-xs text-white/50"><div className="text-purple-200 font-black">{pet.name || id.slice(0, 8)}</div><div>{pet.stage || 'Unknown stage'} / {pet.totalInteractions || 0} interactions</div></div>)}{entries.length === 0 && <div className="text-[10px] text-white/15 uppercase text-center py-8">No ghost pet progress indexed.</div>}</div></section>; }
